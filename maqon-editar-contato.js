(function(){const K='maqon_automation_v1',load=()=>{try{return JSON.parse(localStorage.getItem(K))||{leads:[],events:[]}}catch(e){return{leads:[],events:[]}}},save=d=>localStorage.setItem(K,JSON.stringify(d)),esc=s=>String(s??'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));function render(){let d=load(),id=new URLSearchParams(location.search).get('lead'),l=d.leads.find(x=>x.id===id)||d.leads.find(x=>x.status==='Fechado')||d.leads[0],a=document.getElementById('app');if(!l){a.innerHTML='<h1>MAQON — Editar Contato</h1><div class=card>Nenhum lead encontrado.</div>';return}a.innerHTML=`<h1>MAQON — Editar Contato do Cliente</h1><p>Atualização dos dados usados pelo CRM e pela Central de Comunicação.</p><div class=card><b>${esc(l.nome)}</b><br>${esc(l.empresa||'—')}<br>${esc(l.tipoEquipamento||'—')} | ${esc(l.marcaModelo||'—')}</div><form id=f class=card><label>WhatsApp<input name=whatsapp inputmode=tel placeholder="Ex.: (98) 99999-9999" value="${esc(l.whatsapp||'')}"></label><label>E-mail<input name=email type=email value="${esc(l.email||'')}"></label><button>SALVAR CONTATO</button></form><div id=ok></div>`;f.onsubmit=e=>{e.preventDefault();let db=load(),x=db.leads.find(v=>v.id===l.id),fd=new FormData(f);x.whatsapp=String(fd.get('whatsapp')||'').trim();x.email=String(fd.get('email')||'').trim();db.events=db.events||[];db.events.unshift({tipo:'contato_atualizado',leadId:l.id,quando:new Date().toISOString()});save(db);ok.innerHTML='<b>Contato atualizado com sucesso.</b><br>Agora abra novamente maqon-comunicacao.html para testar o WhatsApp.'}}addEventListener('DOMContentLoaded',render)})();